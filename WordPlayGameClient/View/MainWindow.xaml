<Window x:Class="WordPlayGameClient.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WordPlayGameClient"
        WindowStyle="None"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800" MinWidth="522" MinHeight="420">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="90*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50*" MinWidth="140"/>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="50*" MinWidth="120"/>
            </Grid.ColumnDefinitions>

            <DockPanel Background="{Binding PanelBackgroundColour}" MouseDown="DockPanel_MouseDown" Grid.Row="0" Grid.Column="0">
                <StackPanel Orientation="Horizontal" FlowDirection="LeftToRight">
                    <TextBlock Text=" That word game" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{Binding FontColour}"/>
                </StackPanel>
            </DockPanel>

            <DockPanel Background="{Binding PanelBackgroundColour}" MouseDown="DockPanel_MouseDown" Grid.Column="1" Grid.Row="0">
                <Button Content="  About  " Command="{Binding ShowAboutViewCommand}" Background="{Binding BackgroundColour}" Foreground="{Binding FontColour}" FontSize="20" DockPanel.Dock="Right" FontWeight="Light"/>
                <Button Content=" Settings " Command="{Binding ShowSettingsViewCommand}" Background="{Binding BackgroundColour}" Foreground="{Binding FontColour}" FontSize="20" DockPanel.Dock="Left" FontWeight="Light"/>
                <Button Content=" Main " Command="{Binding ShowMainViewCommand}" Background="{Binding BackgroundColour}" Foreground="{Binding FontColour}" FontSize="20" DockPanel.Dock="Bottom" FontWeight="Light"/>
            </DockPanel>

            <DockPanel Background="{Binding PanelBackgroundColour}" MouseDown="DockPanel_MouseDown" Grid.Column="2" Grid.Row="0">
                <StackPanel Orientation="Horizontal" FlowDirection="RightToLeft">
                    <Button Content="  ✕  " Background="IndianRed" FontSize="20" Command="{Binding ExitAppCommand}"/>
                    <Button Content="{Binding MinimizeIcon}" Background="{Binding BackgroundColour}" FontSize="20" Command="{Binding ToggleFullscreenAppCommand}" Foreground="{Binding FontColour}"/>
                    <Button Content="  -  " Background="{Binding BackgroundColour}" FontSize="20" Command="{Binding MinimizeAppCommand}" Foreground="{Binding FontColour}"/>
                </StackPanel>
            </DockPanel>
        </Grid>

        <ContentControl Grid.Row="1" Content="{Binding MainView}"/>
    </Grid>
</Window>
