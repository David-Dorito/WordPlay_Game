<UserControl x:Class="WordPlayGameClient.View.MainView.GameRoundView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WordPlayGameClient.View.MainView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid Background="{Binding BackgroundColour}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="180"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="1" Background="{Binding PanelBackgroundColour}">
            <Grid.RowDefinitions>
                <RowDefinition Height="115"/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Border BorderThickness="1" BorderBrush="{Binding FontColour}" Grid.Row="1" Margin="5">
                <StackPanel>
                    <TextBlock FontSize="16" Margin="5,2,0,0" Foreground="{Binding FontColour}" Text="Turn order" TextAlignment="Center"/>
                    <Rectangle Height="1" Fill="{Binding FontColour}" HorizontalAlignment="Stretch" Margin="0,4"/>

                    <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding Players, Mode=TwoWay}" Background="Transparent">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Height="30" Background="Transparent">
                                        <TextBlock Text="{Binding Username}" Grid.Column="0" FontSize="16" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="{Binding DataContext.FontColour, RelativeSource={RelativeSource AncestorType=ItemsControl}}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="0">
                <TextBlock FontSize="12" Foreground="{Binding FontColour}">
                    <Run Text=" Word:"/>
                    <Run Text="{Binding SecretWordDisplayText}"/>
                </TextBlock>
                <TextBlock FontSize="12" Foreground="{Binding FontColour}">
                    <Run Text=" Hint:"/>
                    <Run Text="{Binding CurrentGameRound.SelectedHint}"/>
                </TextBlock>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="54"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Margin="5,2,0,2" FontSize="16" Text="{Binding HintInputText, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding HintInputActive}"/>
                    <Button Grid.Column="1" Margin="0,2,5,2" Content=" Submit " FontSize="12" Background="LightGreen" Command="{Binding SubmitHintCommand}" IsEnabled="{Binding HintInputActive}"/>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="54"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Margin="5,2,0,2" FontSize="16" Text="{Binding GuessInputText, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding GuessInputActive}"/>
                    <Button Grid.Column="1" Margin="0,2,5,2" Content=" Guess " FontSize="12"  Background="LightGreen" Command="{Binding SubmitGuessCommand}" IsEnabled="{Binding GuessInputActive}"/>
                </Grid>

                <Button Content="Togge Vote Popup" Background="{Binding BackgroundColour}" Foreground="{Binding FontColour}" Margin="5,2,5,2" Command="{Binding ToggleVotePopup}" BorderBrush="{Binding FontColour}" BorderThickness="1">
                    <Button.Resources>
                        <Style TargetType="Popup">
                            <Setter Property="Placement" Value="Top"/>
                            <Setter Property="StaysOpen" Value="False"/>
                        </Style>
                    </Button.Resources>

                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="Togge Vote Popup" Background="Transparent"/>
                                <Popup Name="VotePopup" IsOpen="{Binding DataContext.IsVotePopupOpen, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                    <Border Background="{Binding DataContext.BackgroundColour, RelativeSource={RelativeSource AncestorType=UserControl}}" Padding="10" CornerRadius="0" BorderBrush="{Binding DataContext.FontColour, RelativeSource={RelativeSource AncestorType=UserControl}}" BorderThickness="1">
                                        <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">
                                            <ItemsControl ItemsSource="{Binding DataContext.VotePopupPlayerList, RelativeSource={RelativeSource AncestorType=UserControl}}" Background="Transparent">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <VirtualizingStackPanel/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Height="30" Background="Transparent">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition/>
                                                                <ColumnDefinition Width="40"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Text="{Binding Username}" Grid.Column="0" FontSize="16" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="{Binding DataContext.FontColour, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                                            <Button Grid.Column="1" Background="IndianRed" Content=" Vote " FontSize="12" FontWeight="Light" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="8,2,0,2" Command="{Binding DataContext.VotePlayerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding ConnectionId}"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
            </StackPanel>

            <Border BorderThickness="1" BorderBrush="{Binding FontColour}" Grid.Row="2" Margin="5">
                <StackPanel>
                    <TextBlock FontSize="16" Margin="5,2,0,0" Foreground="{Binding FontColour}" Text="Waiting" TextAlignment="Center"/>
                    <Rectangle Height="1" Fill="{Binding FontColour}" HorizontalAlignment="Stretch" Margin="0,4"/>

                    <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding WaitingPlayers, Mode=TwoWay}" Background="Transparent">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Height="30" Background="Transparent">
                                        <TextBlock Text="{Binding Username}" Grid.Column="0" FontSize="16" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="{Binding DataContext.FontColour, RelativeSource={RelativeSource AncestorType=ItemsControl}}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Column="0" Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>

            <Border BorderBrush="{Binding FontColour}" BorderThickness="1" Margin="3,3,3,0">
                <TextBlock Text="Game Chat" FontSize="18" TextAlignment="Center" FontWeight="Light" Foreground="{Binding FontColour}" Grid.Row="0"/>
            </Border>

            <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled" Grid.Row="1">
                <ItemsControl ItemsSource="{Binding CurrentGameRound.ShownMessages}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Background="Transparent">
                                <TextBlock Text="{Binding}" FontSize="16" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="{Binding DataContext.FontColour, RelativeSource={RelativeSource AncestorType=ItemsControl}}" TextWrapping="Wrap" Margin="3,0,0,0"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="40"/>
                </Grid.ColumnDefinitions>

                <TextBox Margin="5,0,0,5" MaxLength="255" Grid.Column="0" Text="{Binding MessageInputText}" VerticalContentAlignment="Center"/>
                <Button Grid.Column="1" Background="LightGreen" Margin="0,0,2,5" Content="Send" Command="{Binding SendGameMessageCommand}"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
